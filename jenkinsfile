pipeline {
    agent any
        
    tools {
    maven 'maven' 
  }

    stages {
       stage("Git Checkout."){
          steps{
           git 'https://github.com/sriharshabalasa/flipkart.git'
           }
          }
          
           stage("build "){
          steps{
           sh "mvn -f flipkart/pom.xml clean package"
           }
          }
        
        stage ('Deploy') {
      steps {
        script {
          deploy adapters: [tomcat9(credentialsId: 'tomcat', path: '', url: 'http://13.127.69.5:8080/')], contextPath: '', onFailure: false, war: 'flipkart/target/*.war' 
        }
      }
    }
    }
}
